/**
 * Zaimを開くブラウザコンテキストの共通設定。
 *
 * **Zaimの「最終更新」はブラウザのタイムゾーンで描画される。** subpc のシステムTZはUTCで、
 * 何も指定しないとヘッドレスChromiumもUTCで描く。一方、取り込み側（`parse.ts`）は
 * 表示文字列をJSTとして解釈し、「当日（JST）に更新されたか」を判定している。
 * ずれると9時間ぶん前日扱いになり、**当日更新できた口座まで「更新できなかった口座」として
 * 通知される**（#89。実際に29件中29件が当日でないと判定されていた）。
 *
 * 表示の日付形式（`2026年08月19日 07:20:15`）はZaim側が決めているため、`locale` は指定しない。
 */
export const ZAIM_CONTEXT_OPTIONS = { timezoneId: "Asia/Tokyo" }
